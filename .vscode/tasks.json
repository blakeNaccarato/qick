{
  "version": "2.0.0",
  "type": "shell",
  "problemMatcher": ["$python"],
  "tasks": [
    {
      "label": "create-demo-project",
      "detail": "Create demo project in Vivado 2023.1",
      "icon": { "id": "wrench" },
      "type": "shell",
      "command": "uvx",
      "args": ["--from", "rust-just", "just", "create-demo-project"],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
    {
      "label": "generate-vhdl-ls",
      "detail": "Generate `vhdl_ls.toml` from IPs with VHDL files",
      "icon": { "id": "checklist" },
      "command": "uvx",
      "args": ["--from", "rust-just", "just", "generate-vhdl-ls"],
    }
  ]
}
